# 参与开发

本文说明 TileZhan / TileSlash 的代码和文档贡献流程。

## 基本原则

- 从最新 `main` 创建独立分支，不直接在默认分支开发。
- 一次提交解决一个清晰问题，避免混入无关格式化和重构。
- 先写可验证目标，再实现最小改动。
- 新行为需要测试；行为变化需要同步中文文档。
- 不提交密钥、签名材料、构建产物、本地数据库和临时下载目录。

## 分支与提交

分支建议使用：

```text
codex/<简短主题>
```

提交标题建议采用：

```text
feat: 新增功能
fix: 修复问题
docs: 更新文档
test: 补充测试
chore: 工程维护
```

标题中的类型保留为工具常用标识，具体描述使用中文或团队统一语言。不要用提交信息记录密钥、账号或内部地址。

## 开发与测试

完整环境和编码规则见 [开发指南](docs/development.md)。提交前至少执行与变更范围对应的测试，具体矩阵见 [测试与质量保证](docs/testing.md)。

常规客户端变更：

```powershell
Set-Location frontend
flutter gen-l10n
flutter analyze --no-fatal-infos
flutter test
```

常规服务端变更：

```powershell
Set-Location backend
python -m pytest -q
```

## 合并请求检查清单

- [ ] 变更目标和用户影响说明清楚。
- [ ] 只包含本需求相关文件。
- [ ] 新增和修改的测试全部通过。
- [ ] 三种语言的 ARB key 保持一致。
- [ ] 何切题库修改通过专用校验工具。
- [ ] 没有密钥、凭证、APK、AAB、IPA、keystore 或临时数据。
- [ ] 接口、存储、构建或玩法变化已经更新对应中文文档。
- [ ] `CHANGELOG.md` 已记录用户可见或重要工程变化。
- [ ] GitHub Actions 全部通过。

## 代码审查重点

- 训练结果是否只有在有效作答后计数。
- 关键进度是否真正写入磁盘，而非仅更新界面。
- 存储格式是否向后兼容并有迁移测试。
- 用户可见文案是否来自 ARB。
- 身份、订阅、排行榜和分析接口是否有合适权限。
- 商品 ID 是否与客户端、后端和商店配置一致。
- 错误响应和日志是否可能泄露敏感内容。
- 新功能是否在断网、恢复、重复点击和异常退出下可预测。

## 文档贡献

文档正文使用中文。命令、文件路径、接口路径、代码符号和第三方产品名保持原名，以便读者能够直接在项目中搜索。

修改后检查：

```powershell
git diff --check
git status --short
```

并确认 Markdown 相对链接指向仓库中的真实文件。
