# 安全说明

## 报告安全问题

不要在公开 Issue 中披露可利用的漏洞、真实令牌、私钥或用户数据。优先通过 GitHub 仓库的私密安全报告功能联系仓库维护者，并提供：

- 受影响版本和提交
- 问题出现的模块或接口
- 最小复现步骤
- 实际影响和攻击前提
- 已知缓解方式
- 不包含真实用户凭证的证明材料

维护者确认问题并准备修复前，不公开完整利用细节。

## 敏感信息范围

以下内容不得提交到 Git：

- `.env` 中的真实值
- Firebase 服务账号 JSON、私钥和身份令牌
- RevenueCat API key 与 webhook secret
- Android keystore、密码和别名凭据
- Apple 发布证书、私钥、描述文件和 App Store Connect 私钥
- Google Play 服务账号 JSON
- 用户邮箱、购买凭证、本地训练数据库和带身份的日志

仓库只保存变量名称和空占位符。真实值使用 GitHub Secrets 或部署平台的密钥管理功能。

## 生产安全基线

- `APP_ENV=production`
- `DEBUG=false`
- `ALLOW_DEV_AUTH_BYPASS=false`
- Firebase 三项服务账号配置完整
- RevenueCat webhook secret 已配置
- 跨域来源使用明确白名单，不允许任意来源
- 受保护接口验证 Firebase 身份令牌
- webhook 使用独立密钥验证来源
- 日志不输出身份令牌、私钥和完整购买凭证
- 排行榜、分析和管理接口在生产前补齐身份和权限控制
- 客户端权益不能作为唯一可信订阅依据

## 当前需要特别注意的边界

- 分析仪表盘、分析事件和排行榜接口当前没有完整生产权限控制。
- 部分服务端状态仍保存在进程内存，不能视为可信持久数据。
- 客户端与后端商品 ID 当前不一致，销售默认关闭。
- 客户端网络重试部分逻辑仍是占位实现。
- RevenueCat webhook 路由在生产启用前需要完成代码、密钥验证和自动化测试复核。

这些边界不应通过文档声明来替代代码修复。启用相应生产能力前，必须完成实现、测试和安全审查。

## 依赖与自动化

- 固定依赖前验证版本真实存在。
- 定期检查 Flutter、Python、GitHub Actions 和第三方依赖安全更新。
- 不在自动化日志中打印 Secret 是否存在、长度或解码内容。
- 第三方 Action 应固定到可信版本，并定期审查权限。
- 工作流只授予完成任务所需的最小仓库和商店权限。

## 数据处理

- 只收集功能所需的最少用户数据。
- 新增数据字段时同步隐私政策和商店数据安全表单。
- 提供清晰的数据删除、订阅恢复和账号退出行为。
- 测试、截图和演示使用虚构数据。
